The main theme about the project is actually to draw a star. 
It has many functions like import and turtle which imports the turtle so we can draw. 
Then I created the screen using a variable named paper and the keyword Screen() and turtle.
Then I used the variable pen to create a turtle which creates a pen which draws the star on the screen.
I gave gave the screen a background colour using bgcolor().
And the pen colour using color().
And Made the pen draw withe goto() and x,y coordinates.
That is the last part I used mainloop() keyword to hold the screen.
That is all about my program.
